# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Add one
with `pnpm changeset` describing your change; the release workflow consumes them
to version and publish `@monetizekit/react` to npm on merge to `main`.
